# 5-Minute Demo Script

1. Start at `/builds` and highlight the three active builds and status badges.
2. Open `GW-993-TRB-11` and show the HOLD banner with the blocker note.
3. Walk through tabs: Overview, BOM, Tasks & Events, Documents, Work Orders.
4. In Tasks & Events, point out phase hopping via PAUSE/RESUME/BLOCK events.
5. Show “Current Focus Phase” derived from the most recent IN_PROGRESS item.
6. Go to `/inventory` and show the CRITICAL shortage for `ENG_BEARING_SET_993`.
7. Visit `/parts` and demo SKU search and type filters.
8. Open `/work-orders` and highlight the engine rebuild work order detail.
9. Mention build book placeholder for scanned PDFs in Documents tab.
10. Close by referencing that all data comes from Neon Postgres + Prisma.
